
const Message = () => {
    return (
        <div>
        <h2>I am from msg component file in the components folder of the react portfolio project </h2>
        </div>
    );
};

export default Message;